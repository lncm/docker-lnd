diff --git Dockerfile Dockerfile
index 118ab7f..119072a 100644
--- Dockerfile
+++ Dockerfile
@@ -75,7 +75,8 @@ RUN git verify-tag "${VERSION}" || \
     { git verify-tag --raw "${VERSION}" 2>&1 | grep EXPKEYSIG && echo "Accepting valid signature with an expired key!"; }
 
 # NOTE: needed to have deterministic builds
-RUN go mod edit -go=1.13
+RUN go mod edit -go=1.13 \
+    -replace github.com/btcsuite/btcd=github.com/btcsuite/btcd@v0.20.0-beta.0.20191109022137-037489ef505e
 
 RUN go mod tidy
 
